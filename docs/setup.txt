# Docker Compose commands for Elasticsearch setup

# Start containers
sudo docker compose up -d

# Stop containers
sudo docker compose down

# View running containers
docker ps

# View logs
sudo docker compose logs -f
